# 实施计划

- [x] 1. 建立 TypeScript 模块化单体工程
  - [x] 1.1 初始化 `apps/web`、`apps/api`、`apps/worker` 与共享 packages
    - 配置 workspace、TypeScript、React、Vite、Fastify、统一 lint、格式化、构建和测试命令。
    - 验证 Web、API 和 worker 可以从同一代码库独立构建和启动。
    - _需求: 16.1, 16.2, 16.3_
  - [x] 1.2 建立类型化配置与启动校验
    - 校验数据库、OIDC、方舟、共享 Environment、TOS、模型允许列表和默认配额配置。
    - 保证 `ARK_API_KEY` 只在服务端包可见，缺少必要配置时拒绝进入 ready 状态。
    - _需求: 11.1, 11.4, 12.1, 12.2, 16.6_
  - [x] 1.3 建立共享 API 契约与标准错误结构
    - 在 `packages/contracts` 定义 DTO、错误码、分页结构、Session 状态和前端事件类型。
    - 添加 Schema 校验测试，拒绝 `owner_id`、`tenant_id`、`role` 和未声明字段。
    - _需求: 1.5, 2.4, 16.4, 16.5_

- [x] 2. 建立 PostgreSQL 数据模型与 Repository
  - [x] 2.1 实现用户、登录会话和角色 Schema
    - 创建 `users`、`auth_sessions` 表、索引和迁移，确保 OIDC `sub` 唯一并只保存会话令牌哈希。
    - 添加迁移与 Repository 集成测试。
    - _需求: 1.2, 1.5, 15.1_
  - [x] 2.2 实现平台 Agent、个人 Agent 和默认分配 Schema
    - 创建 `platform_agents`、`personal_agents`、`user_default_agents`，落实唯一约束、状态枚举和单用户单默认分配。
    - 添加平台/个人 Agent 类型约束与分配事务测试。
    - _需求: 2.1, 3.1, 3.6, 4.1, 15.2, 15.5_
  - [x] 2.3 实现 Session、输入附件和产物 Schema
    - 创建 `sessions`、`session_event_cursors`、`session_inputs`、`artifacts`，保证 Session 只引用一种 Agent 类型。
    - 添加归属、归档、删除状态和文件关联约束测试。
    - _需求: 2.1, 5.2, 5.3, 8.3, 9.2, 10.1_
  - [x] 2.4 实现配额、用量、后台作业和审计 Schema
    - 创建 `quota_policies`、`user_quota_overrides`、`usage_ledger`、`background_jobs`、`audit_logs`。
    - 验证用量去重约束和 `FOR UPDATE SKIP LOCKED` 作业领取。
    - _需求: 10.6, 10.7, 13.1, 13.2, 13.3, 13.4, 13.7, 13.8, 16.4_

- [x] 3. 实现认证与应用会话
  - [x] 3.1 实现登录凭据校验流程（原托管身份验证码方案已废弃）
    - 完成凭据提交、统一失败响应和尝试频率限制。
    - 现状（2026-09-11 修订）：删除 `ManagedIdentityAdapter`、`DeterministicManagedIdentityStub` 与 `/auth/email-code`、`/auth/verify`；改为 `packages/auth` 的 scrypt 密码哈希（`password.ts`）与服务端登录 `POST /api/v1/auth/login`，账号由管理员创建。
    - _需求: 1.1, 1.2, 1.3_
  - [x] 3.2 实现 HttpOnly 应用会话与认证中间件
    - 完成登录态创建、续期、注销、禁用用户处理和 `/api/v1/me`。
    - 验证未登录请求无法访问任何业务资源。
    - _需求: 1.2, 1.4, 1.5_
  - [x] 3.3 实现角色守卫和管理员访问边界
    - 仅允许 `admin` 访问平台 Agent、用户分配和配额路由。
    - 添加管理员无法读取用户 Session、附件和产物的路由测试。
    - _需求: 2.6, 15.1, 15.9_

- [x] 4. 实现 Ark Gateway 与测试替身
  - [x] 4.1 实现 Managed Agents 类型化客户端
    - 封装 Agent CRUD、Session CRUD、Events、事件历史、Files 和 Session Resources 接口。
    - 统一携带平台鉴权和请求关联 ID，禁止业务模块直接发起方舟请求。
    - _需求: 2.2, 2.4, 4.8, 6.1, 8.1, 9.1, 12.1_
  - [x] 4.2 实现方舟错误分类、退避与未知结果处理
    - 映射限流、临时故障、版本冲突、RuntimeBusy 和终止状态。
    - 只重试安全请求；写请求结果未知时进入待对账状态。
    - _需求: 4.8, 7.5, 16.5_
  - [x] 4.3 实现可编程 Ark Stub
    - 支持 Agent 版本、Session 状态机、事件历史、SSE、文件、429、5xx 和连接中断。
    - 为后续集成测试提供确定性场景。
    - _需求: 5.1, 6.6, 7.4, 7.5, 16.8_

- [x] 5. 实现统一租户授权层
  - [x] 5.1 实现 Tenant Guard 与租户 Repository 查询约束
    - 所有普通用户资源查询同时使用资源 ID 和当前 `user_id`；平台 Agent 必须校验分配关系。
    - 对不存在和越权资源统一返回 `404 RESOURCE_NOT_FOUND`。
    - _需求: 2.1, 2.2, 2.3, 2.5, 3.5_
  - [x] 5.2 建立跨租户安全测试矩阵
    - 对 Agent、Session、输入附件、产物和用量接口逐一替换另一用户资源 ID。
    - 保证所有越权路径均在调用 Ark Gateway 或 TOS 前失败。
    - _需求: 2.2, 2.3, 2.5, 16.7_

- [x] 6. 实现管理员平台 Agent 管理
  - [x] 6.1 实现平台 Agent 创建、读取和更新服务
    - 使用名称、描述、允许模型、System Prompt 与固定内置工具策略创建平台 Agent。
    - 更新时携带当前方舟版本并记录新版本；冲突时要求刷新。
    - _需求: 4.5, 4.6, 4.7, 5.1, 15.2, 15.3, 15.8_
  - [x] 6.2 实现平台 Agent 启停和受保护删除
    - 支持启用、停用；当存在用户分配或 Session 引用时拒绝删除并返回引用冲突。
    - 覆盖方舟失败、本地失败和待对账状态。
    - _需求: 4.8, 15.4, 15.7_
  - [x] 6.3 实现用户默认平台 Agent 分配
    - 以事务替换用户当前分配，只允许分配已启用的平台 Agent。
    - 分配变化不修改已有 Session。
    - _需求: 3.1, 3.4, 3.6, 3.7, 15.5, 15.6_
  - [x] 6.4 实现平台 Agent 与分配管理 API
    - 完成 `/admin/platform-agents`、`/admin/users`、默认分配接口及审计日志。
    - 验证响应不包含用户消息、附件、产物或敏感配置。
    - _需求: 12.2, 15.1, 15.9, 16.4_

- [x] 7. 实现普通用户 Agent 管理
  - [x] 7.1 实现可用 Agent 列表和默认选择规则
    - 区分已分配平台 Agent 与个人 Agent，返回 `kind`、`editable` 和当前版本。
    - 按最近使用 Agent、默认平台 Agent 顺序解析新任务选择；无默认项时返回明确阻塞状态。
    - _需求: 3.2, 3.4, 3.5, 4.1_
  - [x] 7.2 实现个人 Agent 创建、编辑和删除
    - 校验个人 Agent 配额和模型允许列表，固定内置工具及权限策略。
    - 实现版本冲突、方舟失败和本地对账状态。
    - _需求: 4.2, 4.3, 4.4, 4.5, 4.6, 4.7, 4.8, 5.1, 13.1_
  - [x] 7.3 验证 Agent 版本语义
    - 确认新 Session 使用最新版本，已有 Session 保持创建版本，并仅只读展示版本。
    - 确认普通用户无法浏览、比较或回滚版本。
    - _需求: 5.2, 5.3, 5.4, 5.5, 5.6_

- [x] 8. 实现 Session 创建与生命周期 API
  - [x] 8.1 实现空闲 Session 创建编排
    - 校验用户、Agent 权限、配额和固定 Environment，使用 Agent 最新版本创建方舟 Session。
    - 保存实际 Agent 版本、资源归属和初始 `idle` 状态。
    - _需求: 3.3, 5.2, 5.4, 6.1, 6.2, 11.1, 11.2, 11.3_
  - [x] 8.2 实现 Session 列表与详情
    - 支持未归档/已归档筛选，返回状态、标题、Agent 类型、名称和只读版本。
    - 禁止终止 Session 继续接收消息。
    - _需求: 5.5, 6.6, 6.7, 14.4_
  - [x] 8.3 实现消息追加和中断
    - 将消息转换为 `user.message`，支持运行中排队状态并处理 RuntimeBusy。
    - 将中断转换为 `user.interrupt`，等待状态事件更新本地投影。
    - _需求: 6.3, 6.4, 6.5_

- [x] 9. 实现 SSE 代理、恢复和事件投影
  - [x] 9.1 实现首连 SSE 代理
    - 在允许发送首条消息前确认上游 SSE 已建立，并在客户端断开时释放上游连接。
    - 转发原始事件 ID、类型和规范化 UI 事件。
    - _需求: 7.1, 7.2, 7.3, 7.4_
  - [x] 9.2 实现 SSE 重连与历史补偿
    - 先建立上游流并缓冲，再拉取历史，按 `event.id` 合并和去重。
    - 添加历史与实时事件交叉、重复、断线和终止场景契约测试。
    - _需求: 7.6, 7.7, 16.8_
  - [x] 9.3 实现事件驱动状态与错误投影
    - 处理 `thinking`、工具调用/结果、`running`、`idle`、`rescheduled`、`terminated` 和 `session.error`。
    - 区分可恢复与不可恢复错误并更新 Session 投影。
    - _需求: 6.4, 6.5, 7.2, 7.3, 7.4, 7.5_
  - [x] 9.4 补记：实现 SSE `ready` 状态帧
    - 上游流建立后在 `apps/api/src/app.ts` 下发不带 `id:` 的 `event: ready` 帧，载荷取自本地 Session 投影（`sessionId`、`status`、`agentName`、`agentVersion`），不调用方舟接口。
    - _需求: 7.8_
  - [x] 9.5 补记：实现工具载荷脱敏与摘要
    - `packages/contracts/src/redact.ts` 以纯函数提供凭据擦除、路径白名单处理与工具参数摘要/结果预览截断。
    - _需求: 7.3, 7.9_

- [x] 10. 实现输入附件上传与挂载
  - [x] 10.1 实现私有暂存上传
    - 以 `purpose=agent` 调用 Files API，保存用户归属、文件元数据和过期时间。
    - 规范化文件名和挂载路径，拒绝路径穿越。
    - _需求: 8.1, 8.3, 12.2_
  - [x] 10.2 实现 Session 创建时附件挂载
    - 只允许挂载当前用户尚未绑定的上传文件，并作为只读 Session Resource 传入。
    - 上传或挂载失败时阻止任务启动并保留可重试状态。
    - _需求: 8.2, 8.5, 8.6_
  - [x] 10.3 实现输入附件局部展示和清理
    - 仅在所属 Session 展示输入附件，排除在“我的文件”之外。
    - worker 清理过期且未绑定的上传记录和上游文件。
    - _需求: 8.3, 8.4, 8.5_

- [x] 11. 实现 Agent 产物管理
  - [x] 11.1 实现 Session 产物同步与索引
    - 通过 `scope_id` 查询 `/mnt/session/outputs/` 产物并建立用户、Session 和存储对象索引。
    - 幂等更新名称、类型、大小和生成时间。
    - _需求: 9.1, 9.2, 9.4_
  - [x] 11.2 实现产物列表、筛选和安全下载
    - 支持按来源 Session 筛选，并在租户校验后提供短期签名地址或流式下载。
    - 验证响应不暴露其他用户对象键或长期公共 URL。
    - _需求: 9.2, 9.3, 9.5, 13.6_
  - [x] 11.3 实现产物删除作业
    - 删除 TOS 对象和本地索引；失败时保留可对账状态并由 worker 重试。
    - 添加重复删除和部分失败测试。
    - _需求: 9.6, 9.7_
  - [x] 11.4 补记：实现产物内联预览
    - `apps/web/src/artifacts.ts` 判定可预览类型并限制预览体积；`components/ArtifactViewer.tsx` 内联渲染 Markdown、文本、CSV、图片，HTML 使用仅 `allow-scripts` 的沙箱 iframe。
    - _需求: 9.9_

- [x] 12. 实现 Session 归档与永久删除 Saga
  - [x] 12.1 实现归档和恢复
    - 只更新应用层 `archived_at`，不修改方舟 Session、事件或产物。
    - 验证默认列表隐藏归档项且可恢复。
    - _需求: 10.1, 10.2_
  - [x] 12.2 实现永久删除请求和二次确认契约
    - API 只在明确确认后创建删除作业，并向 UI 返回 pending 状态。
    - _需求: 10.3_
  - [x] 12.3 实现可恢复的 Session 删除 Saga
    - 运行中先中断，再删除方舟 Session、TOS 产物和本地关联数据。
    - 为每个步骤记录完成标记，支持幂等重试和 `deletion_failed` 状态。
    - _需求: 10.4, 10.5, 10.6, 10.7_

- [x] 13. 实现配额与用量控制
  - [x] 13.1 实现有效配额解析与管理员覆盖
    - 合并系统默认值和用户覆盖值，提供管理员调整 API。
    - 验证平台 Agent 不计入个人 Agent 上限。
    - _需求: 13.1, 13.3, 13.4, 13.8, 13.9_
  - [x] 13.2 实现并发和每日 Session 配额预占
    - 使用数据库事务防止并发穿透，创建失败时释放预占。
    - 临界判定前与方舟状态对账。
    - _需求: 13.2, 13.3_
  - [x] 13.3 实现事件驱动用量账本
    - 从模型、工具和状态事件累计 Token、工具调用和运行时长，并按事件唯一键去重。
    - 添加事件重放和月度窗口测试。
    - _需求: 13.4, 13.7_
  - [x] 13.4 实现额度耗尽行为
    - 达到月度 Token 上限后拒绝新 Session 和消息，观察到运行中越限时发起中断。
    - 保持历史查看、归档、删除和产物下载可用。
    - _需求: 13.5, 13.6_

- [x] 14. 实现普通用户工作台外壳
  - [x] 14.1 实现响应式导航与任务侧栏
    - 桌面端使用窄侧栏，移动端使用抽屉，展示主导航和未归档 Session 状态。
    - 使用 Lucide 图标、可访问名称、Tooltip 和键盘导航。
    - _需求: 14.1, 14.2, 14.4, 14.6, 14.7_
  - [x] 14.2 实现新建任务输入区
    - 提供 Agent 选择、附件、文本输入、发送和运行状态反馈。
    - 无默认 Agent、上传失败、额度不足时显示明确阻塞状态。
    - _需求: 3.2, 3.3, 3.4, 8.6, 13.5, 14.1, 14.5_
  - [x] 14.3 实现登录与应用会话界面
    - 完成邮箱密码提交、凭据错误/限流/服务不可用的差异化提示、退出和未认证跳转。
    - 现状（2026-09-11 修订）：验证码步骤已移除；登录页改为单步邮箱密码表单。
    - _需求: 1.1, 1.2, 1.3, 1.4_
  - [x] 14.3a 补记：实现管理员用户管理界面（2026-09-11）
    - `AdminPage.tsx` 的用户页支持查看全部账号（邮箱、角色、状态、是否已设密码）、创建账号和重置密码；登录成功后的密码明文不落库、不回显。
    - _需求: 17.1, 17.2, 17.3, 17.4, 17.5, 17.6_
  - [x] 14.4 补记：实现深色模式与主题偏好
    - `apps/web/src/theme.ts` 管理浅色/深色/跟随系统偏好并持久化到本机；`index.html` 内联脚本在首帧绘制前解析存储偏好，避免闪烁。
    - _需求: 14.8_
  - [x] 14.5 补记：实现命令面板
    - `components/CommandPalette.tsx` 提供键盘搜索与跳转（新建任务、各页面、会话）及外观切换命令。
    - _需求: 14.9_

- [x] 15. 实现普通用户功能页面
  - [x] 15.1 实现 Agent 列表与个人 Agent 编辑器
    - 分区展示平台 Agent 和个人 Agent；平台 Agent 只读，个人 Agent 支持创建、编辑和删除。
    - 不展示版本历史、比较或回滚控件。
    - _需求: 3.5, 4.1, 4.2, 4.3, 4.4, 5.5, 5.6_
  - [x] 15.2 实现 Session 时间线页面
    - 展示消息、思考状态、折叠工具活动、状态、错误、版本、中断、归档和删除操作。
    - 保持事件加载和状态变化时输入区布局稳定。
    - _需求: 5.5, 6.4, 6.5, 6.6, 7.1, 7.2, 7.3, 7.4, 7.5, 14.6_
  - [x] 15.3 实现“我的文件”页面
    - 只展示 Agent 产物，支持 Session 筛选、元数据、下载和删除状态。
    - _需求: 8.4, 9.2, 9.3, 9.4, 9.5, 9.6_
  - [x] 15.4 实现未开放能力入口
    - 为 Skills、MCP、Vault 和上下文管理展示明确的未开放状态，不渲染可操作表单。
    - _需求: 11.5, 12.4, 12.5, 14.2, 14.3_

- [x] 16. 实现管理员界面
  - [x] 16.1 实现平台 Agent 管理页面
    - 支持创建、编辑、启停和受保护删除，展示版本与引用冲突。
    - _需求: 15.2, 15.3, 15.4, 15.7, 15.8_
  - [x] 16.2 实现用户默认 Agent 与配额页面
    - 展示用户身份摘要、当前默认平台 Agent 和配额，不展示用户内容。
    - 支持分配已启用 Agent 和修改配额。
    - _需求: 13.8, 15.5, 15.6, 15.9_
  - [x] 16.3 验证管理员前端与 API 权限
    - 普通用户不能加载管理员路由，管理员页面不能导航到用户内容。
    - _需求: 2.6, 15.1, 15.9_

- [x] 17. 完成安全、可观测性与部署
  - [x] 17.1 配置 Web 安全控制
    - 启用同源 CORS、CSP、安全响应头、Cookie 策略、接口速率限制和日志脱敏。
    - 验证普通用户无法配置平台密钥、Environment 或非允许模型。
    - _需求: 11.2, 11.3, 11.4, 12.2, 12.3, 12.6_
  - [x] 17.2 实现结构化日志、审计和健康检查
    - 记录 request ID、用户、资源、结果和 Ark Request ID，不记录敏感凭据或内容。
    - 提供 live/ready 探针并覆盖数据库与必要配置检查。
    - _需求: 16.4, 16.6_
  - [x] 17.3 实现容器镜像和运行配置
    - 构建单一 OCI 镜像，为 API 和 worker 提供独立启动命令与健康检查。
    - 提供 PostgreSQL、TOS、OIDC、方舟和网络白名单的环境配置模板。
    - _需求: 11.1, 11.4, 16.1, 16.2, 16.3_

- [x] 18. 完成端到端验收与故障验证
  - [x] 18.1 验证普通用户完整工作流
    - 覆盖登录、默认平台 Agent、新建任务、附件、SSE、多轮对话、中断、个人 Agent、产物、归档和删除。
    - _需求: 1.1, 3.3, 4.2, 6.1, 6.3, 6.5, 6.7, 8.1, 8.2, 9.5, 10.1, 10.5_
  - [x] 18.2 验证管理员完整工作流
    - 覆盖平台 Agent 创建、分配、更新版本、停用、删除冲突及用户配额调整。
    - _需求: 3.6, 3.7, 5.1, 15.2, 15.4, 15.5, 15.6, 15.7, 15.8_
  - [x] 18.3 验证多用户隔离与成本保护
    - 使用两个普通用户和一个管理员执行完整跨租户矩阵、并发竞争及额度耗尽场景。
    - _需求: 2.1, 2.2, 2.3, 2.5, 13.1, 13.2, 13.5, 13.6, 16.7_
  - [x] 18.4 验证故障恢复与生产构建
    - 注入方舟限流、5xx、SSE 断线、删除部分失败和数据库重启，验证重试、去重、对账和告警。
    - 构建并启动生产镜像，验证 API 与 worker 健康检查。
    - _需求: 4.8, 7.5, 7.6, 7.7, 9.7, 10.6, 10.7, 16.5, 16.6, 16.8_
